# gp1_limitcycle_mm_gse154989

Notebook-first repo for Group Project 1 (limit-cycle cell-cycle model, *Mus musculus*, GSE154989).

## Run order
01 → 02 → 03 → 04 → 05 → 06 → 07 → 08

Artifacts are saved under `outputs/` and `data/processed/`. Tunables live in `configs/params.yaml`.
